pluginManagement {
    repositories {
        mavenLocal()
        maven { url = uri("https://files.minecraftforge.net/maven") }
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "fabric-modloader"
